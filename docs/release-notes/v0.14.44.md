# v0.14.44

## Nouveautés

- Un mode `Review rapide` permet maintenant de parcourir une par une les fiches visibles de toutes les recherches et des nouveautés, ainsi que les fiches manga ouvertes dans les onglets du workspace.
- La fenêtre de review réunit couverture, informations, description, auteurs, tags, œuvres sources, sources disponibles, correspondances potentielles et miniatures. Tous les liens ouvrent leur cible dans un nouvel onglet sans interrompre la session.
- La section `Options > Review rapide` permet de choisir les éléments affichés, la taille et le nombre de colonnes des miniatures, leur vitesse de défilement au clavier, ainsi que le nombre de fiches suivantes à précharger.
- Une catégorie de raccourcis dédiée contrôle les fiches et les miniatures. Les appuis longs sont pris en charge avec une durée globale configurable ; par défaut, `Droite` avance et un appui long sur `Droite` ajoute le bookmark avant d'avancer.
- Les couvertures et miniatures peuvent être agrandies dans une visionneuse réutilisable avec zoom à la molette, contrôles et déplacement de l'image.
- La vue Nouveautés propose une fenêtre de suivi des scans profonds, organisée par tags favoris et scrapers. Chaque source peut être dépliée pour consulter sa progression par langue, les pages atteintes, son état et ses éventuels blocages.
- Le point de reprise d'un tag, d'un scraper ou d'une seule de ses sources peut être réinitialisé après confirmation afin de reparcourir entièrement son catalogue. Ce reset conserve les favoris et l'historique des cards vues.

## Améliorations

- Les listes de lecture peuvent maintenant être nommées. Leur nom est proposé depuis la série reconnue par le parseur
  de titres ou, à défaut, depuis l'auteur majoritaire. La reprise démarre aussi au dernier manga réellement commencé
  dans la liste ; une progression restée à la première page est ignorée.
- La review utilise la liste réellement filtrée et choisit en priorité la source correspondant au classement de langues de l'utilisateur pour les résultats fusionnés.
- Afficher une fiche en review la compte comme vue. Les auteurs et tags favoris sont signalés, tandis que l'état déjà lu, bookmarké ou présent dans une liste reprend les indications et confirmations des fiches classiques.
- Les cards, fiches et reviews rapides signalent maintenant la progression des séries avec `Série commencée` ou `Précédents non lus`. Cette protection, activée par défaut dans les réglages, demande aussi confirmation avant de bookmarker une suite dont les chapitres précédents ne sont pas marqués comme lus.
- La détection reconnaît les plages de chapitres : une lecture `1-8` couvre par exemple le chapitre `3`.
- La fenêtre exploite presque toute la hauteur disponible sous la barre de titre, bloque le défilement de l'arrière-plan et réserve une colonne plus large à la couverture.
- La galerie latérale affiche de grandes miniatures sans chevauchement, prend en charge les sprites des scrapers, place `Voir plus` à la fin et aligne les raccourcis Haut/Bas sur les lignes réelles. Un double appui Bas bloqué en fin de galerie charge la suite.
- Les fiches et reviews rapides chargent automatiquement la première galerie de miniatures lorsqu'elle se trouve sur une page séparée. Une fiche qui propose à la fois des vignettes et des chapitres les range dans deux onglets, avec les vignettes affichées en premier.
- Les actions de chaque chapitre sont regroupées sous son titre et son URL. Lorsqu'aucune image de chapitre n'est disponible, la carte reste compacte et n'affiche plus de zone vide.
- Le navigateur de scraper décrit maintenant chaque étape d'un chargement — récupération, extraction des cards et enrichissement des fiches — avec une progression chiffrée lorsque plusieurs fiches sont analysées.
- Les fiches ouvertes directement dans un nouvel onglet chargent aussi automatiquement leur première galerie externe, sans demander un clic préalable sur `Voir plus`.
- La review rapide indique le nombre de chapitres et propose une action `Lecture` pour les sources dont les pages dépendent des chapitres. Elle reprend le chapitre consulté le plus récemment à la page enregistrée ou démarre au premier chapitre en l'absence de progression.
- Les couvertures suivantes sont préchargées en priorité, décodées et gardées en mémoire afin que le changement de fiche soit immédiat. Les détails et miniatures continuent ensuite jusqu'à la profondeur configurée.
- Le démarrage de développement réutilise les compilations incrémentales, recharge automatiquement le renderer et peut réparer une installation Electron incomplète. Le script de réinstallation lance désormais l'application indépendamment puis rend immédiatement la main.
- Les résultats d'une page auteur et des correspondances auteur proposent une vue `Par série`. Elle regroupe les cards en chapitres fusionnés, rassemble les œuvres isolées dans `One Shot` et permet d'ouvrir chaque série dans une vraie vue de correspondance manga déjà préremplie, sans lancer de scraping.
- Le classement d'une card peut être corrigé en modifiant simultanément sa série et son chapitre. Le champ de série reste libre tout en proposant les séries existantes selon la saisie, et la détection automatique peut être restaurée à tout moment.
- Une correspondance préremplie peut être révisée puis lancée à la demande avec le rejeu habituel. Les nouveaux résultats, corrections et invalidations sont ensuite répercutés dans la vue globale de toutes les séries.
- La révision d'une recherche de correspondances auteur permet maintenant d'ajouter directement des fiches manga reconnues par les scrapers. Elles sont conservées avec la recherche et analysées en priorité lors du prochain approfondissement afin de découvrir davantage de noms et de pages auteur.

## Corrections

- Relancer un scan profond avec un plafond de pages plus élevé reprend désormais le parcours au-delà de l'ancienne limite mise en cache. Augmenter ou désactiver la limite de rejets par langue débloque également la source concernée.
- Le lecteur se replace en haut avant d'afficher une nouvelle page afin qu'un scroll de navigation ne soit plus interrompu par le changement d'image.
- Les fiches déjà bookmarkées et les correspondances potentielles utilisent la même vérification dans la review rapide que dans la fiche complète.
- Les bookmarks d'un même scraper sont maintenant reconnus même lorsque l'URL de la fiche diffère uniquement par un slash final. L'ajout et la suppression réutilisent aussi cette identité afin d'éviter les doublons.
- La review rapide signale également un bookmark équivalent trouvé sur une autre source grâce au titre et à l'auteur, même lorsque les métadonnées ajoutées au titre diffèrent.
- Les protections de lecture réutilisent désormais l'analyse tolérante de la recherche de correspondances, ce qui rétablit les alertes sur les titres contenant des événements, auteurs, traductions ou métadonnées complexes.
- Dans les correspondances manga, les chapitres nommés traduits sont regroupés grâce aux titres bilingues qui relient leurs différents sous-titres. Les variations de casse, d'espacement et de ponctuation comme `Child-Bearing` et `Childbearing` fusionnent également, sans confondre les chapitres décimaux. Cette détection reste limitée à cette vue et ne produit pas d'alerte de série globale.
- Les badges d'alerte restent stables pendant le chargement des lectures et réutilisent leur cache lors d'une mise à jour, supprimant leur clignotement et le premier calcul redondant.
