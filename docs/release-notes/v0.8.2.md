# v0.8.2

## Correction

- La recherche multi-sources fusionne mieux les resultats qui designent le meme titre avec une petite variation de romanisation.
- Les titres alternatifs separes par `|` ou `/` peuvent maintenant matcher avec une seule difference de caractere quand les auteurs detectes sont compatibles.

## A savoir

- La fusion reste prudente : deux resultats avec des auteurs detectes contradictoires ne sont pas regroupes par ce nouveau fallback.
