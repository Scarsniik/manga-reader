# Présent négatif poli

Le présent négatif poli dit qu'une action ou un état n'est pas vrai, avec un niveau de politesse plus élevé.

## Comment le former

- Verbe : radical en `ます` + `ません` : `食べる` -> `食べません`, `読む` -> `読みません`.
- `する` : `しません`.
- `来る` : `来ません`.
- Adjectif en い : `高くありません` ou `高くないです`.
- Adjectif en な : `静かじゃありません`, `静かではありません` ou `静かじゃないです`.

## À reconnaître

`ません` est la négation polie de `ます`. Pour les adjectifs, `ありません` est la forme polie de `ない`.

## Liens utiles

- Version neutre : [présent négatif](grammar:form:present-negative).
- Version positive polie : [présent affirmatif poli](grammar:form:present-affirmative-polite).
- Version passée polie : [passé négatif poli](grammar:form:past-negative-polite).
