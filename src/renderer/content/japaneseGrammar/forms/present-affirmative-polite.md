# Présent affirmatif poli

Le présent affirmatif poli garde le sens positif, mais rend la phrase plus polie.

## Comment le former

- Verbe : radical en `ます` + `ます` : `食べる` -> `食べます`, `読む` -> `読みます`.
- `する` : `します`.
- `来る` : `来ます`.
- Adjectif en い : forme de base + `です` : `高いです`.
- Adjectif en な : base + `です` : `静かです`.

## À reconnaître

La présence de `ます` ou `です` indique généralement le poli. La forme reste non-passée si elle ne contient pas `た`.

## Liens utiles

- Version neutre : [présent affirmatif](grammar:form:present-affirmative).
- Version négative polie : [présent négatif poli](grammar:form:present-negative-polite).
- Version passée polie : [passé affirmatif poli](grammar:form:past-affirmative-polite).
